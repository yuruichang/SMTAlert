<Window x:Class="SMTAlert.ZKBMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SMTAlert"
        WindowStyle="None"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Topmost="True"
        Visibility="Visible"
        MinWidth="300" MinHeight="150"
        Title="{DynamicResource ZKB_Title}"
        Height="400" Width="420"
        AllowsTransparency="True">
    <Window.Resources>
        <local:ZKBBackgroundConverter x:Key="zkbBGConverter" />
        <local:ZKBForegroundConverter x:Key="zkbBGFConverter" />
        <Style x:Key="ZkbColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF2A2A2A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Window.Background>
        <SolidColorBrush Opacity="0.2" Color="Black" x:Name="windowBackground" />
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Grid.Row="0" PreviewMouseDown="ZKBMonitor_Window_Move"
                Opacity="0.5" ToolTip="Drag to move" BorderThickness="0" Background="Transparent">
            <Image Source="/Images/overlay_move.png" Stretch="Fill" />
        </Button>

        <TextBlock Grid.Column="1" Grid.Row="0"
                   Background="Transparent" Name="zkb_TitleTextBlock"
                   HorizontalAlignment="Center" TextAlignment="Center"
                   FontSize="10" Opacity="0.5" Margin="1"
                   Foreground="White" Text="{DynamicResource ZKB_Title}" />

        <Button Grid.Column="2" Grid.Row="0" PreviewMouseDown="ZKBMonitor_Window_Close"
                Width="20" HorizontalAlignment="Right" Background="Transparent"
                Opacity="0.5" ToolTip="Close" BorderThickness="0">
            <Image Source="/Images/overlay_close.png" Stretch="Fill" />
        </Button>

        <DataGrid x:Name="ZKBKillList" Grid.Row="1" Grid.ColumnSpan="3"
                  AreRowDetailsFrozen="True" Cursor="Hand"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" SelectionUnit="FullRow"
                  GridLinesVisibility="None" MouseDoubleClick="ZKBKillList_MouseDoubleClick"
                  PreviewMouseLeftButtonDown="ZKBKillList_PreviewMouseLeftButtonDown"
                  ColumnHeaderStyle="{StaticResource ZkbColumnHeaderStyle}"
                  Background="Transparent" Opacity="0.85">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="ColTime"
                                   Binding="{Binding KillTime, StringFormat='{}{0:HH:mm:ss}'}" Width="60"
                                   IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{DynamicResource ZKB_Column_Time}" />
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="ColSystem"
                                   Binding="{Binding SystemName}" Width="*"
                                   IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{DynamicResource ZKB_Column_System}" />
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="ColAlliance"
                                   Binding="{Binding VictimAllianceName}" Width="*"
                                   IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{DynamicResource ZKB_Column_Alliance}" />
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="ColShipType"
                                   Binding="{Binding ShipTypeDisplay}" Width="*"
                                   IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{DynamicResource ZKB_Column_ShipType}" />
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="ColValue"
                                   Binding="{Binding TotalValueDisplay}" Width="80"
                                   IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                    <DataGridTextColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{DynamicResource ZKB_Column_Value}" />
                        </DataTemplate>
                    </DataGridTextColumn.HeaderTemplate>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                            <Setter Property="Margin" Value="0,0,4,0" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Setter Property="Background" Value="{Binding Converter={StaticResource zkbBGConverter}}" />
                    <Setter Property="Foreground" Value="{Binding Converter={StaticResource zkbBGFConverter}}" />
                    <Setter Property="FontSize" Value="10" />
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{DynamicResource ZKB_CtxOpen}" Click="ContextMenu_OpenZKB_Click" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</Window>
